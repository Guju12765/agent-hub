{
  "_comment": "Default hooks for agent-hub. Injected into settings.json on hire.",
  "PreCompact": [{
    "matcher": "manual|auto",
    "hooks": [{
      "type": "prompt",
      "prompt": "[Memory] PreCompact triggered. Run /memory-summarization now to save session state, daily learnings, and durable memories before context is cleared."
    }],
    "description": "Prompt Claude to save memories before compaction"
  }]
}
